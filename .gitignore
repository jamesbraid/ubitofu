__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
# reconcile plan-prelude scratch (always deleted; belt-and-suspenders)
ubitofu-reconcile-*.tf
# tofu working state (never commit real state/plans)
.terraform/
*.tfstate
*.tfstate.*
tofu.plan
*.tfplan
# local scratch / real-controller output (keep the public repo synthetic-only)
/scratch/
/local/

# subagent-driven-development scratch
.superpowers/
.coverage
.claude/settings.local.json
# mutmut working directory (generated per run, not committed)
mutants/
docs/superpowers/
